## Drug Combination Prediction Output

This directory is initially empty, but will collect the results of the drug combination prediction for each dataset. When the prediction code is run, a separate directory is created for each dataset, and within that directory, subdirectories are created for each cell state in each dataset.